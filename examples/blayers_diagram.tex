\documentclass[11pt]{article}
\usepackage{amsmath}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{lmodern}
\usepackage[T1]{fontenc}

% Color palette
\definecolor{modelcol}{RGB}{52, 120, 200}   % blue   — model form / latent vars
\definecolor{distcol}{RGB}{220, 100, 30}    % orange — distributions
\definecolor{argscol}{RGB}{150, 50, 200}    % purple — data / call args

\begin{document}

% --- BLayers call syntax -----------------------------------------------------
\[
  \underbrace{%
    \textcolor{modelcol}{\texttt{AdaptiveLayer}}%
  }_{\substack{\textcolor{modelcol}{\text{\small model form}}\\[2pt]
               \textcolor{modelcol}{\text{\scriptsize (hierarchical prior)}}}}
  \underbrace{%
    \textcolor{distcol}{%
      \texttt{(scale\_dist{=}HalfNormal,\ coef\_dist{=}Normal)}%
    }%
  }_{\substack{\textcolor{distcol}{\text{\small prior distributions}}\\[2pt]
               \textcolor{distcol}{\text{\scriptsize (swappable at construction)}}}}
  \underbrace{%
    \textcolor{argscol}{%
      \texttt{("beta",\ x,\ units{=}2)}%
    }%
  }_{\substack{\textcolor{argscol}{\text{\small name,\ data,\ output dim}}\\[2pt]
               \textcolor{argscol}{\text{\scriptsize (supplied at call time)}}}}
\]

\bigskip

% --- Generative model --------------------------------------------------------
\[
  \begin{array}{rcl@{\quad}l}
    \textcolor{modelcol}{\lambda}
      &\textcolor{modelcol}{\sim}& \textcolor{distcol}{\mathrm{HalfNormal}}\textcolor{modelcol}{(}\textcolor{distcol}{1}\textcolor{modelcol}{)}
      & \multirow{2}{*}{$\Bigg\}\;\textcolor{modelcol}{\texttt{AdaptiveLayer}}$} \\[4pt]
    \textcolor{modelcol}{\beta}
      &\textcolor{modelcol}{\sim}& \textcolor{distcol}{\mathrm{Normal}}\textcolor{modelcol}{(}\textcolor{distcol}{0}\textcolor{modelcol}{,\;}\textcolor{modelcol}{\lambda}\textcolor{modelcol}{)}
      & \\[10pt]
    \sigma
      &\sim& \mathrm{HalfNormal}(1)
      & \multirow{2}{*}{$\Bigg\}\;\texttt{gaussian\_link}$} \\[4pt]
    y
      &\sim& \mathrm{Normal}(\textcolor{modelcol}{\beta} \cdot \textcolor{argscol}{x},\ \sigma)
      &
  \end{array}
\]

\end{document}
